#ifndef IG_COMMON_INCLUDE_PL
#define IG_COMMON_INCLUDE_PL

#include <functional>
#include <regex>
#include <string>
#include <vector>

using std::distance;
using std::hash;
using std::regex;
using std::regex_search;
using std::smatch;
using std::sregex_iterator;
using std::sregex_token_iterator;
using std::string;
using std::vector;

#endif
